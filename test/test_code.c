#include <stdio.h>

int main() {
    int a = 10;
    float b = 3.14;
    double c = 1.2e-3;
    int h = 0xFF;

    // Comparaciones y logicos
    if (a >= 10 && h != 0) {
        printf("Valor: %d\n", a);
    }

    /* Comentario multilinea
       con operadores: == != <= >= && || !
    */

    char* s = "hola\tmundo\n";
    return 0;
}
